#ifndef INCLUDE_HPP
#define INCLUDE_HPP

/**
 * @file Includes.hpp
 * @brief Centralized header file that includes standard C++ libraries used throughout the project.
 */

// Standard library headers
#include <iostream>    ///< For input/output stream operations
#include <string>      ///< For std::string and related functions
#include <vector>      ///< For std::vector container
#include <list>        ///< For std::list container
#include <set>         ///< For std::set container
#include <map>         ///< For std::map container
#include <chrono>      ///< For time-related operations and measurements
#include <cstdlib>     ///< For random number generation and memory management
#include <ctime>       ///< For handling date and time
#include <fstream>     ///< For file input/output
#include <sstream>     ///< For string stream manipulation
#include <algorithm>   ///< For standard algorithms like sort, find, etc.
#include <memory>      ///< For smart pointers like std::unique_ptr, std::shared_ptr
#include <cmath>       ///< For mathematical operations
#include <iomanip>     ///< For formatted I/O

using namespace std;

#endif // INCLUDE_HPP